This is the VInput Chinese Input Method framework. This repo contains the library and a (for test purpose) CLI interface.

TODO:
1. switch to a binary dict format (like capnp or msgpack) and probably use mmap to read.